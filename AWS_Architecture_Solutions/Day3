# 70번
새 객체의 PUT에 대한 S3의 일관성 모델은 '쓰기 후 읽기 일관성'이다.

# 71번
S3는 초당 3,500 번의 PUT을 지원한다.

#73번
미국 동부(버지니아 북부)에서 만들어진 S3버킷의 고유한 도메인은 다른 리전과는 다르게 'S3.amazonaws.com'이다.

#74번
S3와 리전은 '-'로 분리한다.

#75번
AWS는 S3에서 '가상 호스팅 방식' 및 '경로 방식' 이 두가지 버킷 URL 방식을 지원한다.
가상 호스팅 방식 URL은 http://bucket.s3-aws-region/amazonaws.com
경로 방식 URL은 http://s3-aws-region.amazonaws.com/bucket-name

#77번
온프레미스(고객) 사이트와 S3사이에서(=s3버킷과 상호작용) 특정한 구성을 통해 데이터를 로컬 캐싱할 수 있게 하는 가상 어플라이언스를 AWS Storage Gateway이다.
다만, 하드웨어 어플라이언스로는 사용할 수 없다.

# 79번
S3버킷은 특정 리전에 만들지만 버킷 이름은 글로벌하게 고유하며 글로벌 네임 스페이스에 있어야 한다. 객체 기반이며, 단일 객체는 5TB로 제한되지만, 버킷의 총 스토리지 용량은 무제한이다.

# 81번
S3의 모든 객체에는 키, 값, 버전 ID가 있다.

#82번
S3 버킷에 있는 객체가 실수로 삭제되지 않게 하는 가장 좋은 방법은 '버킷에 대한 MFA Delete'를 활성화하는 것이다.

# 83번
MFA Delete 요청으로 사용되는 HTTP 요청 헤더는 'x-amz-mfa'이다. 아마존 특화 요청 헤더는 모두 x-amz로 시작된다.
MFA Delete를 활성화하면 '버킷의 버전 관리 상태 변경'을 하거나, '영구적으로 객체 버전 삭제'를 하는 작업에 적용할 수 있다.
* S3버킷을 삭제한다거나 객체의 메타 데이터를 삭제하는 작업은 하지 않는다.
MFA Delete 인증 코드는 구글 authenticator 같은 가상 MFA 장치 또는 하드웨어에서 가져온다.
MFA Delete를 활성화할 수 있는 사람은 오직 루트계정만 가능하다. 버킷을 생성한 AWS Management Console 사용자라도 루트 사용자가 아니면 버킷에서도 활성화할 수 없다.

