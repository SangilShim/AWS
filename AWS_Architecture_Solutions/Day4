#61
MFA Delete를 활성화할 수 있는 사람은 오직 루트계정만 가능하다. 버킷을 생성한 AWS Management Console 사용자라도 루트 사용자가 아니면 버킷에서도 활성화할 수 없다.

이와는 다르게
S3버킷에 버전 관리를 활성화 할 수 있는 사람은 버킷 소유자, 버킷을 소유한 루트계정, 버킷에 권한을 가진 모든 IAM 사용자이다.

# 62
S3에 저장된 객체에 연결된 것에는 '이름, 값(데이터), 버전 ID, 메타데이터가 있다.
* 인증 ID나 버전 기록(버전관리를 설정하지 않으면 존재할 수 없으므로 항상 맞는 답이 아니기 때문이다.)은 아니다!!

# 63
AWS Management Console을 사용해 객체에 메타데이터를 추가하는 AWS메커니즘은 태그이며, 모든 메타 데이터는 이름-값 페어인 태그로 AWS Management Console에서 입력한다.

# 64
S3에서 객체의 모든 버전을 저장하지 않는 예외 경우는 없다. 객체가 MFA Delete를 통해 삭제되든, 객체의 모든 버전, 현재 버전이 삭제되든 모든 버전의 객체가 저장된다.

# 65
S3 버킷에서 버전 관리가 활성화된 후에는 비활성화하거나 해제할 수 없다. 버전 관리는 일시 중단할 수는 있지만 실제로 버전 관리 기능은 해제되지 않으며 이전 버전은 유지된다.

# 66
Cloud Front는 원본 서버에서 정적 파일을 캐시해서 사용자 또는 클라이언트로 전달한다. EC2 또는 다른 웹 서버로부터 CloudFront를 통해 동적 콘텐츠를 제공할 수도 있다.
Cloud Front는 스토리지 시설이 아니라 배포 메커니즘이기 때문에 객체 기반 스토리지는 아니다.
