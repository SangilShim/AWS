#61
MFA Delete를 활성화할 수 있는 사람은 오직 루트계정만 가능하다. 버킷을 생성한 AWS Management Console 사용자라도 루트 사용자가 아니면 버킷에서도 활성화할 수 없다.

이와는 다르게
S3버킷에 버전 관리를 활성화 할 수 있는 사람은 버킷 소유자, 버킷을 소유한 루트계정, 버킷에 권한을 가진 모든 IAM 사용자이다.

# 62
S3에 저장된 객체에 연결된 것에는 '이름, 값(데이터), 버전 ID, 메타데이터가 있다.
* 인증 ID나 버전 기록(버전관리를 설정하지 않으면 존재할 수 없으므로 항상 맞는 답이 아니기 때문이다.)은 아니다!!

# 63
AWS Management Console을 사용해 객체에 메타데이터를 추가하는 AWS메커니즘은 태그이며, 모든 메타 데이터는 이름-값 페어인 태그로 AWS Management Console에서 입력한다.

# 64
S3에서 객체의 모든 버전을 저장하지 않는 예외 경우는 없다. 객체가 MFA Delete를 통해 삭제되든, 객체의 모든 버전, 현재 버전이 삭제되든 모든 버전의 객체가 저장된다.

# 65
S3 버킷에서 버전 관리가 활성화된 후에는 비활성화하거나 해제할 수 없다. 버전 관리는 일시 중단할 수는 있지만 실제로 버전 관리 기능은 해제되지 않으며 이전 버전은 유지된다.

# 66
Cloud Front는 원본 서버에서 정적 파일을 캐시해서 사용자 또는 클라이언트로 전달한다. EC2 또는 다른 웹 서버로부터 CloudFront를 통해 동적 콘텐츠를 제공할 수도 있다.
Cloud Front는 스토리지 시설이 아니라 배포 메커니즘이기 때문에 객체 기반 스토리지는 아니다.

# 67
Cloud Front가 제공하는 데이터의 원본은 'origin servers'(원본 서버의 콘텐츠)인데, 일반적으로 정적 파일 및 동적 응답이다. 이러한 원본 서버는 주로 정적 콘텐츠는 S3 버킷이고,
동적 콘텐츠의 경우는 EC2 인스턴스다. EC2 대신에 ECS에서 실행되는 Docker 컨테이너를 사용하기도 한다.

# 68
Cloud Front는 전 세계의 엣지 로케이션에 콘텐츠를 저장하고 캐싱한 콘텐츠를 사용자에게 제공(배포)한다.
따라서 전 세계 엣지 로케이션의 집합을 Cloud Front의 '배포'라고 한다. 
( 수 : 엣지로케이션 > 가용 영역 > 리전 )
# 69
Cloud Front는 EC2와 직접 연결하지 않고 ELB와 연결해서 콘텐츠를 배포할 수 있으며, 
Route 53 레코드 세트와도 연결해서 콘텐츠를 배포할 수 있다. 이를 통해 콘텐츠가 여러 인스턴스에서 제공될 수 있다.
즉 ELB(elastic load balancer)와 Route 53 레코드 세트가 일반적으로 원본 서버가 될 수 있다.
* 반면에 SQS 구독 엔드포인트나 SNS 주제 검색 엔드포인트와 같은 경우는 그럴 수 없다.

# 70
클라우드 프론트를 사용하는 웹 애플리케이션에서 사용자에게 제공되는 콘텐츠를 저장하는 것은 엣지 로케이션과 EC2 인스턴스가 있다.
엣지 로케이션은 Cloud Front 배포에서 캐시도니 콘텐츠를 저장하는 데 사용된다.그리고 EC2 인스턴스는 프로세스로 콘텐츠를 제공할 수 있다.
* Route 53은 Amazon DNS 서비스다. 가용 영역은 콘텐츠 저장 장치가 아니라 가상 데이터 센터이다.
